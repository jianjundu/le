<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3 
  http://maven.apache.org/xsd/assembly-1.1.3.xsd">
  
  <id>${project.artifactId}-assembly-${project.version}</id>
  <includeBaseDirectory>false</includeBaseDirectory>
  <formats>
    <format>war</format>
  </formats>
  <fileSets>
    <!-- 将 target/classes 下的文件输出到 WEB-INF/classes -->
    <fileSet>
      <directory>${project.build.outputDirectory}</directory> <!-- target/classes -->
      <outputDirectory>WEB-INF/classes</outputDirectory>
      <excludes>
        <!-- 排除conf目录下的properties文件 -->
        <exclude>**/conf/*.properties</exclude>
        <!-- 排除env目录 -->
        <exclude>**/env/</exclude>
      </excludes>
    </fileSet>
    <!-- 把相应环境下的properties输出到WEB-INF/classes/conf -->
    <fileSet>
      <directory>${project.build.outputDirectory}/conf</directory>
      <outputDirectory>WEB-INF/classes/conf</outputDirectory>
      <includes>
      	<include>config_${env}.properties</include>
      </includes>
    </fileSet>
    <!-- 把env目录下相应环境下的spring配置文件输出到WEB-INF/classes -->
    <fileSet>
      <directory>${project.build.outputDirectory}/env/${env}/</directory>
      <outputDirectory>WEB-INF/classes</outputDirectory>
    </fileSet>
    <!-- 将 webapp 下的文件输出到 WAR 包 -->
    <fileSet>
      <directory>${project.basedir}/src/main/webapp</directory>
      <outputDirectory>/</outputDirectory>
    </fileSet>
  </fileSets>
  <!-- 将项目依赖的JAR包输出到 WEB-INF/lib -->
  <dependencySets>
    <dependencySet>
      <outputDirectory>WEB-INF/lib</outputDirectory>
      <useProjectArtifact>false</useProjectArtifact>
    </dependencySet>
  </dependencySets>
</assembly>